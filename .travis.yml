language: c++
compiler: g++

matrix:
  include:
    - os: osx
      osx_image: xcode10

before_install:
  - brew upgrade gcc -q
  - CC=gcc-7
  - CXX=g++-7

install:
  - brew install unittest-cpp -q

script:
  - make test
  - ./build/test

after_success:
  - make clean
